import { ExportToExcelComponent } from './export-to-excel.component';

describe('ExportToExcelComponent', () => {
    it('should create an instance', () => {
        const directive = new ExportToExcelComponent();
        expect(directive).toBeTruthy();
    });
});